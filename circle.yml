dependencies:
    cache_directories:
        - ".tox"
    override:
        - "tox --notest"
    pre:
        - pip install tox codecov
        - pyenv versions
        - pyenv local 2.7.12 3.4.4 3.5.3 3.6.2

test:
    override:
        - tox

    post:
        - tox -e coverage-report
        - codecov
